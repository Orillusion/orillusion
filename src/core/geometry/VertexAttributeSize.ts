/**
 * @internal
 */
export enum VertexAttributeSize {
    'u32' = 1,
    'f32' = 1,
    'vec2<f32>' = 2,
    'vec3<f32>' = 3,
    'vec4<f32>' = 4,

    'float32' = 1,
    'float32x2' = 2,
    'float32x3' = 3,
    'float32x4' = 4,
}
